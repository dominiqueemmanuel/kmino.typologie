% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discretisation.r
\name{discretisation}
\alias{discretisation}
\title{Une fonction permettant de discrétiser des variables d'une table.}
\usage{
discretisation(data, var, n = 5, method = c("Amplitudes �gales",
  "Effectifs �gaux"), labelled = TRUE, new_name = NULL, ...)
}
\arguments{
\item{data}{est une\code{data.frame}.}

\item{var}{est un vecteur de \code{character} de noms de colonnes `data` dont les types sont tous \code{numeric}.}

\item{n}{est un entier strictement positif représentant le nombre de classe à créer.}

\item{method}{est un \code{character} représentant la méthode. Valeurs possibles : à définir avec Régis car les sépcifications sont incomplètes sur ce point (Je mets néanmoins une valeur par défaut \code{"Amplitudes égales"}).}

\item{labelled}{un booléan. Si \code{TRUE} le recodage sera un \code{factor} dont les \code{levels} devront indiquer la signification du regadage (par exemple les \code{levels} pourront être \code{c("Age<=20","Age dans ]20 ; 50]", "Age > 50")}). Si \code{FALSE} le recodage sera un entier (entre \code{1} et \code{n}).}

\item{new_name}{soit \code{NULL} soit un vecteur de \code{character} de même longueur que \code{var} (permet de soit remplacer les variables par leurs recodages, soit créer de nouvelles variables).}

\item{...}{sont des paramètres complémentaires dont la nature dépend de la méthode (à définir donc selon les besoin des développements).}
}
\description{
Une fonction permettant de discrétiser des variables d'une table.
}
\examples{
#Pas d'exemple pour le moment
}

