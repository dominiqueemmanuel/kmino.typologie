% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse factorielle TEST ED.R, R/factor_analysis.r
\name{factor_analysis}
\alias{factor_analysis}
\title{Une fonction permettant de réaliser toutes les analyses factorielles.}
\usage{
factor_analysis(data, var_act, var_supp = NULL,
  method = c("ACP/ACM globale", "ACP/ACM r�p�t�es par blocs",
  "Analyse Canonique", "ACP Rotation", "AFM Duale"), weight = NULL,
  group_var_id = NULL, group_ind_id = NULL, nb_axe = "Automatique", ...)

factor_analysis(data, var_act, var_supp = NULL,
  method = c("ACP/ACM globale", "ACP/ACM r�p�t�es par blocs",
  "Analyse Canonique", "ACP Rotation", "AFM Duale"), weight = NULL,
  group_var_id = NULL, group_ind_id = NULL, nb_axe = "Automatique", ...)
}
\arguments{
\item{data}{est une\code{data.frame}.}

\item{var_act}{est un vecteur de \code{character} de noms de colonnes `data` constituant les variables actives de l'analyses./}

\item{var_supp}{est un vecteur de \code{character} de noms de colonnes `data` constituant les variables passive de l'analyses./}

\item{method}{est un \code{character} représentant la méthode
\code{"ACP/ACM global"} : suppose que les variables sont qualitatives ou quantitative
\code{"ACP/ACM répétées par groupe"} : suppose que les variables sont qualitatives ou quantitative
\code{"Analyse Canonique"} : suppose que les variables sont toutes quantitatives
\code{"ACP Rotation"} : suppose que les variables sont toutes quantitatives
\code{"AFM Duale"} : suppose que les variables sont toutes quantitatives}

\item{weight}{soit \code{NULL} (pas  de pondération des indivudus) soit un vecteur de \code{numeric} positif de même longueur que \code{nrow(data)} .}

\item{group_var_id}{soit \code{NULL} (pas  groupe de variable) soit un vecteur de \code{integer} de longueur  \code{length(var)} indiquant pour chaque variable à quel groupe elle appartient.}

\item{group_ind_id}{soit \code{NULL} (pas  groupe d'individu) soit un vecteur de \code{integer} (utile uniquement si  \code{method = "AFM Duale"}) de longueur  \code{ncol(data)} indiquant pour chaque individu à quel groupe il appartient.}

\item{nb_axe}{soit \code{"Automatique"} (le nombre d'axes devra être fixé automatiquement selon un bon critère à définir) soit un nombre dans \code{]0, 1]} (cela représent la proportion d'information à conserver) un entier supérieur ou égal à 2 (cela représente le nombre d'axes à conserver)}

\item{...}{sont des paramètres complémentaires dont la nature dépend de la méthode (à définir donc selon les besoin des développements).}

\item{var}{est un vecteur de \code{character} de noms de colonnes `data` constituant les variables actives de l'analyses. (Les autres variables seront donc à ignorer complètement dans cette partie)/}

\item{data}{est une\code{data.frame}.}

\item{method}{est un \code{character} représentant la méthode
\code{"ACP/ACM global"} : suppose que les variables sont qualitatives ou quantitative
\code{"ACP/ACM répétées par blocs"} : suppose que les variables sont qualitatives ou quantitative
\code{"Analyse Canonique"} : suppose que les variables sont toutes quantitatives
\code{"ACP Rotation"} : suppose que les variables sont toutes quantitatives
\code{"AFM Duale"} : suppose que les variables sont toutes quantitatives}

\item{weight}{soit \code{NULL} (pas  de pondération des indivudus) soit un vecteur de \code{numeric} positif de même longueur que \code{nrow(data)} .}

\item{group_var_id}{soit \code{NULL} (pas  groupe de variable) soit une liste donc chaque élément est une liste de type list(var_act=...,var_supp=...)}

\item{group_ind_id}{soit \code{NULL} (pas  groupe d'individu) soit un vecteur de \code{integer} (utile uniquement si  \code{method = "AFM Duale"}) de longueur  \code{ncol(data)} indiquant pour chaque individu à quel bloc il appartient.}

\item{nb_axe}{soit \code{"Automatique"} (le nombre d'axes devra être fixé automatiquement selon un bon critère à définir) soit un nombre dans \code{]0, 1]} (cela représent la proportion d'information à conserver) un entier supérieur ou égal à 2 (cela représente le nombre d'axes à conserver)}

\item{...}{sont des paramètres complémentaires dont la nature dépend de la méthode (à définir donc selon les besoin des développements).}
}
\description{
Une fonction permettant de réaliser toutes les analyses factorielles.

Une fonction permettant de réaliser toutes les analyses factorielles.
}
\examples{
#Pas d'exemple pour le moment
#Pas d'exemple pour le moment
}

